OTA 待办清单（ESP32-S3）

【分区与容量】
- [ ] 确认分区表包含：factory + ota_0 + ota_1 + otadata。
- [ ] 确认每个 OTA 分区都能容纳“最大固件体积 + 余量”。
- [ ] OTA 前检查：固件大小（Content-Length）<= 下一升级分区大小。
- [ ] 若服务端不提供大小（未知总长度），按策略决定是否拒绝升级。

【镜像合法性】
- [ ] 校验镜像头/目标芯片为 ESP32-S3。
- [ ] 校验 app 描述信息可解析（版本号等）。
- [ ] 增加防降级策略（可选，推荐）。
- [ ] 以 esp_ota_end() 成功作为切换启动分区的硬条件。

【传输与安全】
- [ ] 生产环境使用 HTTPS。
- [ ] 配置服务端证书或证书包，开启 TLS 校验。
- [ ] DNS/网络失败采用有上限的重试与退避。
- [ ] 明确记录 HTTP 状态码与 OTA 传输错误。

【升级流程】
- [ ] 仅写入“下一 OTA 分区”，禁止写当前运行分区。
- [ ] 完整下载并写入成功后再调用 esp_ota_set_boot_partition()。
- [ ] 仅在切换分区成功后执行重启。
- [ ] 进度展示：已知总长显示百分比；未知总长显示已下载字节数。

【回滚与自检】
- [ ] 保持回滚链路：PENDING_VERIFY -> VALID / ROLLBACK。
- [ ] 自检优先验证“运行健康”，不要只依赖瞬时网络状态。
- [ ] Wi-Fi 可达性建议改为可选项（离线设备不应因无网回滚）。
- [ ] 自检失败时记录明确原因并允许自动回滚。

【版本策略】
- [ ] 明确当前固件版本唯一来源。
- [ ] 明确远端版本来源格式（version.txt 或 JSON）。
- [ ] 版本比较规则固定（major.minor.patch）。
- [ ] 远端版本为空/非法时不得触发升级。

【资源与并发】
- [ ] OTA 期间记录并校验可用堆内存（前后对比）。
- [ ] OTA 写入期间避免重负载 UI/后台任务并发。
- [ ] 增加“OTA 进行中”互斥，防止重复触发。
- [ ] 校核 OTA 任务栈大小（watermark）。

【可观测性】
- [ ] 增加结构化日志：开始/检查/下载/写入/切换/重启/回滚。
- [ ] 失败原因分类记录（网络/容量/镜像/写入/校验）。
- [ ] UI 显示状态机（空闲/检查中/升级中/失败/成功）。
- [ ] 各阶段加超时，避免长期卡在“updating...”。

【发布前验证】
- [ ] 正常路径测试：N -> N+1 升级成功。
- [ ] 异常镜像测试：应在切换分区前被拒绝。
- [ ] OTA 过程中断电测试：设备可恢复。
- [ ] 首次启动自检通过/失败回滚链路测试。
